# Local dev: Vite proxy (vite.config.ts) routes /api → http://localhost:8000
# No env vars needed for local development.

# Only set VITE_API_URL if running the frontend against a remote backend
# without Vercel rewrites (e.g., running `vite preview` against staging).
VITE_API_URL=
